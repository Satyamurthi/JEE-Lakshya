<?xml version="1.0" encoding="UTF-8"?>
<configuration>
    <system.webServer>
        <!-- CORS headers are managed directly by PHP scripts (api/db.php and api/router.php) -->
    </system.webServer>
</configuration>
